{
	"defaults": {
		"standard": "WCAG2AA",
		"runners": ["axe"],
		"timeout": 30000,
		"wait": 1000,
		"actions": ["wait for element #main h1 to be visible"],
		"chromeLaunchConfig": {
			"args": ["--no-sandbox", "--disable-setuid-sandbox"]
		}
	},
	"urls": ["http://localhost:4173/"]
}
